<UserControl x:Class="Missile_Falls_Project.Views.PictureView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Missile_Falls_Project.Views"
        mc:Ignorable="d"
        Loaded="UserControl_Loaded">
        <Grid Background="#FF3A3A3A">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button Content="Load Images"

            Margin="10" Padding="10 5"

            HorizontalAlignment="Right"

            Click="Button_Click"/>
            <ListBox ItemsSource="{Binding MyPictures}"

                ScrollViewer.HorizontalScrollBarVisibility="Disabled"

                Grid.Row="1" >
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid MaxWidth="200" Margin="10">
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition Height="Auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                            <Image Source="{Binding Url}" />
                            <TextBlock Text="{Binding Title}" HorizontalAlignment="Center" Grid.Row="1" Margin="0 5"/>
                        <TextBlock x:Name="reportShow"/>
                    </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
</UserControl>
